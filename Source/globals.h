// A central place for all the global variables.
// They will be available to all the files as extern.

volatile int CAN_ACQUIRE = 1; // use if you need to block the program during some steps
volatile unsigned short LED_BLINK_STATUS = 0;